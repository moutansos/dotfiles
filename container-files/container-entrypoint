#!/bin/bash
set -euo pipefail

HOME_ROOT="/home/ben"
HOME_TEMPLATE="/opt/home-template.tar.gz"
HOME_MARKER="$HOME_ROOT/.container-home-initialized"

mkdir -p "$HOME_ROOT"
tar --skip-old-files -xzf "$HOME_TEMPLATE" -C "$HOME_ROOT"
touch "$HOME_MARKER"

/usr/local/bin/init-source

exec "$@"
